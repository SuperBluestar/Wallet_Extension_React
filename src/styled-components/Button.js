import styled from 'styled-components';
import { paddingSm, paddingLg } from "../constants/dimentions";

const Button = styled.button`
    border-radius: 1000px;
    padding: ${paddingSm}px ${paddingSm}px;
    border: none;
    cursor: pointer;
`;

export default Button;