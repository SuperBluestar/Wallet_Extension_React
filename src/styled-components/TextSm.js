import styled from 'styled-components';

import { fontSizeSm, lineHeightSm } from "../constants/fonts";

const TextSm = styled.div`
    font-family: Loboto;
    font-weight: 400;
    font-size: ${fontSizeSm}px;
    line-height: ${lineHeightSm}px;
`;

export default TextSm;