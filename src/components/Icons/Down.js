import Down from '../../assets/down.png';
import { iconWidth } from "../../constants/dimentions";

export default () => (
    <img style={{
        width: iconWidth,
        height: iconWidth
    }} src={Down} alt="Down"/>
)