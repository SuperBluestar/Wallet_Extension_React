import styled from 'styled-components';

import { fontSizeLg, lineHeightLg } from "../constants/fonts";

const TextLg = styled.div`
    font-family: Loboto;
    font-weight: 400;
    font-size: ${fontSizeLg}px;
    line-height: ${lineHeightLg}px;
`;

export default TextLg;