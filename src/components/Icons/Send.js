import Send from '../../assets/send.png';
import { iconWidth } from "../../constants/dimentions";

export default () => (
    <img style={{
        width: iconWidth,
        height: iconWidth
    }} src={Send} alt="Send"/>
)