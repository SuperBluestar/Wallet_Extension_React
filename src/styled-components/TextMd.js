import styled from 'styled-components';

import { fontSizeMd, lineHeightMd } from "../constants/fonts";

const TextMd = styled.div`
    font-family: Loboto;
    font-weight: 400;
    font-size: ${fontSizeMd}px;
    line-height: ${lineHeightMd}px;
`;

export default TextMd;